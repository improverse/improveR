% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loadHistory.R
\name{loadParentalDescendant}
\alias{loadParentalDescendant}
\title{Load the parental/descendant relationships of a resource}
\usage{
loadParentalDescendant(ident, from = pwd())
}
\arguments{
\item{ident}{Character. Resource identifier to resolve (e.g. UUID, name, or any
identifier accepted by \code{loadResource()}).}

\item{from}{Character. Path used to resolve the identifier (defaults to the
current working directory via \code{pwd()}).}
}
\value{
A dataframe with the following columns:
\itemize{
\item \code{type} (character)
\item \code{resourceId} (character)
\item \code{entityId} (character)
\item \code{entityVersionId} (character)
\item \code{path} (character)
\item \code{name} (character)
\item \code{data} (list)
}

The \code{data} column contains a nested dataframe with:
\itemize{
\item \code{resourceId} (character)
\item \code{resourceVersionId} (character)
\item \code{nodeType} (character)
\item \code{name} (character)
\item \code{deleted} (logical)
\item \code{entityId} (character)
\item \code{entityVersionId} (character)
\item \code{revisionId} (character)
\item \code{lastModifiedOn} (numeric)
\item \code{lastModifiedByName} (character)
\item \code{status} (character)
\item \code{fileSize} (integer)
\item \code{hasChildren} (logical)
\item \code{hasChildrenIncludingFiles} (logical)
\item \code{path} (character)
\item \code{outdatedLink} (logical)
\item \code{workingFile} (logical)
\item \code{lastModifiedOnDate} (POSIXct)
}
}
\description{
Returns the parental descendants of the resource.
Parental descendants are
\itemize{
\item copies (modified or not) of the resource to other locations
\item output files which were created by the resource due to its role as an input file.
}
}
\details{
The function uses caching to improve performance. The cache persists for the
entire R session. If parental descendant relationships change on the server,
use \code{refreshParentalDescendant()} to refresh the cached data, or use
\code{unloadParentalDescendant()} to remove the cached data.
}
\examples{
\dontrun{
# by UUID
loadParentalDescendant("123e4567-e89b-12d3-a456-426614174000")

# by resource name within a specific repository path
loadParentalDescendant("my-resource-name", from = "/path/to/repo")
}

}
\seealso{
\code{\link[=refreshParentalDescendant]{refreshParentalDescendant()}}, \code{\link[=unloadParentalDescendant]{unloadParentalDescendant()}}, \code{\link[=loadResource]{loadResource()}}
}
