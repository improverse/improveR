% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loadFullChildResources.R
\name{loadFullChildResources}
\alias{loadFullChildResources}
\title{loads all child resources  by the resourceId,  entity ID or entity version ID
the childResources REST call does not return all the fields, like run status for steps, just the fields all resources have in common, with this function, the full resources are loaded
It Uses Caching
the results are returned as a data frame or a list of data frames
the dates are also converted to posix dates via convertImproveTimestampToPosix
resourceId can be a list}
\usage{
loadFullChildResources(ident, from = pwd())
}
\arguments{
\item{ident}{the resource id or the entity id of the resource}

\item{from}{used if a relative path is used}
}
\value{
A data frame with one row per resource, carrying \code{resourceId}, \code{entityId} and
the child resources, each with their own children nested in \verb{$data} - use \code{\link[=strip]{strip()}} to
unwrap a single one. \code{NULL} when \code{ident} resolves to no resource. For several idents
the result of all of them, merged.
}
\description{
arguments:
}
\references{
ics1085
}
