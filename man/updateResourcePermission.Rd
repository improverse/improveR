% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permissions.R
\name{updateResourcePermission}
\alias{updateResourcePermission}
\title{Update an Existing Permission on a Resource}
\usage{
updateResourcePermission(
  ident,
  aclId,
  memberId,
  visible = TRUE,
  read = TRUE,
  modify = FALSE,
  changeRights = FALSE,
  inherit = TRUE,
  orderNr = NULL,
  rightsArea = 1L,
  from = pwd()
)
}
\arguments{
\item{ident}{Identifier of the resource. Can be a path, resource ID, or entity ID.}

\item{aclId}{Character. The ID of the ACL entry to update (from \code{getResourcePermissions()}).}

\item{memberId}{Character. The ID of the user or group.}

\item{visible}{Logical. Whether the member can see the resource exists.}

\item{read}{Logical. Whether the member can read the content.}

\item{modify}{Logical. Whether the member can modify the content.}

\item{changeRights}{Logical. Whether the member can change permissions.}

\item{inherit}{Logical. Whether this ACL entry inherits to child resources. Default \code{TRUE}.}

\item{orderNr}{Integer or NULL. The ordering priority for this ACL entry. Lower numbers
are evaluated first. If \code{NULL}, the server default is used.}

\item{from}{Root path for resolving relative paths. Defaults to \code{pwd()}.}
}
\value{
A list with the updated ACL entry details, or \code{NULL} on failure.
}
\description{
Updates an existing ACL entry on a resource. Unlike \code{setResourcePermission}
which creates a new entry (POST), this uses PUT to modify an existing ACL entry.
This is needed for changing rights ordering (\code{orderNr}).
}
\examples{
\dontrun{
acl <- getResourcePermissions("/Projects/MyProject")
# Move an ACL entry to highest priority
updateResourcePermission("/Projects/MyProject",
  aclId = acl$id[1], memberId = acl$memberId[1],
  visible = TRUE, read = TRUE, modify = TRUE,
  changeRights = FALSE, orderNr = 1)
}
}
\seealso{
\code{\link{setResourcePermission}}, \code{\link{removeResourcePermission}}, \code{\link{getResourcePermissions}}
}
