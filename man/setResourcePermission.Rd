% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permissions.R
\name{setResourcePermission}
\alias{setResourcePermission}
\title{Set a Permission on a Resource}
\usage{
setResourcePermission(
  ident,
  memberId,
  visible = TRUE,
  read = TRUE,
  modify = FALSE,
  changeRights = FALSE,
  inherit = TRUE,
  orderNr = 1L,
  rightsArea = 1L,
  from = pwd()
)
}
\arguments{
\item{ident}{Identifier of the resource. Can be a path, resource ID, or entity ID.}

\item{memberId}{Character. The ID of the user or group to grant permissions to.}

\item{visible}{Logical. Whether the member can see the resource exists.}

\item{read}{Logical. Whether the member can read the content.}

\item{modify}{Logical. Whether the member can modify the content.}

\item{changeRights}{Logical. Whether the member can change permissions.}

\item{inherit}{Logical. Whether this ACL entry inherits to child resources. Default \code{TRUE}.}

\item{orderNr}{Integer. The ordering priority for this ACL entry. Lower numbers
are evaluated first. Must be unique per resource. Default \code{1}.}

\item{rightsArea}{Integer. The rights area: 1 = Primary, 2 = Publish. Default \code{1}.}

\item{from}{Root path for resolving relative paths. Defaults to \code{pwd()}.}
}
\value{
A list with the created ACL entry details, or \code{NULL} on failure.
}
\description{
Creates an ACL entry on a resource, granting specific rights to a user or group.
Uses POST to create a single new ACL entry. The \code{orderNr} and \code{rightsArea}
fields are required by the server DTO (ics769).
}
\examples{
\dontrun{
setResourcePermission("/Projects/MyProject", memberId = "group-id",
  visible = TRUE, read = TRUE, modify = TRUE, changeRights = FALSE)
}
}
\seealso{
\code{\link{removeResourcePermission}}, \code{\link{getResourcePermissions}},
\code{\link{replaceResourcePermissions}}
}
