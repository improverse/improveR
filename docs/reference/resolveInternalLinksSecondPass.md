# Resolve internal links in second pass after all steps are created

Resolve internal links in second pass after all steps are created

## Usage

``` r
resolveInternalLinksSecondPass(internalLinks, importWF)
```

## Arguments

- internalLinks:

  Data frame of internal links.

- importWF:

  Data frame with fullName and newEntityId columns.
