# Reloads the Reviews

Reloads the Reviews

## Usage

``` r
refreshReviews()

updateReviews(...)
```

## Arguments

- ...:

  For backwards compatibility with the deprecated `update*` alias; not
  used by `refresh*` itself.

## Value

A data frame of reviews, or `NULL` if none exist.

## References

ics348
