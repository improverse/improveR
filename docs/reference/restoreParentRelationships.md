# Restore parent relationships after all steps are created

Restore parent relationships after all steps are created

## Usage

``` r
restoreParentRelationships(importWF)
```

## Arguments

- importWF:

  Data frame with parentFullName, fullName, and newEntityId columns.
