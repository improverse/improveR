# Refresh token using registered refresher

Refreshes the access token using the active token refresher plugin

## Usage

``` r
refreshToken(alwaysRefresh = FALSE)
```

## Arguments

- alwaysRefresh:

  Logical. If TRUE, refreshes even if token hasn't expired

## Value

Logical indicating success
