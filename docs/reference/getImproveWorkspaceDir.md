# Get improve workspace directory

Gets the workspace directory for user files and projects.

## Usage

``` r
getImproveWorkspaceDir()
```

## Value

Character string with path to workspace directory
