# Generate link mapping template from outside links

Generate link mapping template from outside links

## Usage

``` r
generateLinkMappingTemplate(allOutsideLinks)
```

## Arguments

- allOutsideLinks:

  Data frame of all outside links (full version, with
  ident/version/filehash/name).

## Value

Data frame of link mappings, or NULL if no links.
