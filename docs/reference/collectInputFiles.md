# Collect input files (non-link) from workflow step data

Collect input files (non-link) from workflow step data

## Usage

``` r
collectInputFiles(workflowDf)
```

## Arguments

- workflowDf:

  Data frame of workflow steps.

## Value

Data frame of input files, or NULL if none found.
